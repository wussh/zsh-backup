# Default sensitive file exclusion patterns
# One glob pattern per line. Lines starting with # are comments.
# These patterns are matched against the basename of the file being tracked.

*_rsa
*_ed25519
*_ecdsa
*_dsa
*.pem
*.key
*.p12
*.pfx
*password*
*secret*
*token*
*credential*
*.env
.netrc
# User-defined exclusions below this line
